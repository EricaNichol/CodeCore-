var a = prompt("Enter Numbers");

var array = a.trim().split(" ");

var sum = 0;

for(var i = 0; i < array.length; i++) {
  sum = sum + parseInt(array[0]);
});


var sum = prompt("enter a list of nmber").split("").reduce(function(first_value, next_value) { return parseInt(First_value) + parseInt(next_value) });

$()
